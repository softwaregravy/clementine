# Resume — verify the doc cascade, merge, start the build

**Status:** done · **For:** a staff-level Claude Code session (the role `CLAUDE.md` describes; use the strongest model available) · **Kickoff:** `Read docs/handoffs/2026-09-20-resume.md and follow it.` · **Writes:** nothing of its own beyond commits; flips this file to `done` when the merge lands.

You are resuming the thread that ran the Phase 1 readiness review on 2026-09-20. Read this whole file before doing anything.

## Where things stand

Branch `phase1-readiness-review`, off `main` (`498fe0a`). Commits on it, in order:

1. `1be29c8` — `docs/mvp-readiness.md` (the readiness review) and five real API pulls under `docs/fixtures/open-data/`.
2. `762761b` — `DECISIONS.md` §9, **DEC-064 to DEC-081**, the readiness decisions in their final, John-confirmed form; `docs/open-questions.md` pruned; readiness banner updated.
3. `966e8a1` (then moved) — `docs/handoffs/2026-09-20-cascade.md`, the editorial brief that brings every other doc into line with DEC-064–081. Another session executes it and writes `docs/handoffs/2026-09-20-cascade-report.md`.

**What was decided, in one breath** (the log is binding; this is orientation): Phase 1 classifies nothing — every violation the city says is owed counts, parking was the minimum scope not a ceiling, copy says "tickets" never "parking tickets" (DEC-065). `amount_due` stays a string; open ⇔ present, non-empty, not `"0"` (DEC-064). System failures — drift, truncation, 4xx, non-array bodies, timeouts — are silent to the subscriber, ERROR in the log, and fail the cron run via a non-zero exit; **there is no Alert send**, Phase 1 has two send types, Welcome and Digest (DEC-071, DEC-077). Drift is a run-level summary check, not per-row logic (DEC-069). No retries, no canary, no nickname (DEC-072, DEC-074, DEC-075). Schema is phone/plate/state with DB check constraints whose patterns are fixed at implementation (DEC-076). P1–P20 and A2–A4 accepted (DEC-078). No Twilio account exists yet; creating it is John's step zero (DEC-079). Sustained-outage and empty-dataset silence are accepted, named risks (DEC-081).

## What the last session learned the hard way

- **A smaller-model session ignored its guardrails.** Asked to record answers in a scratch file and touch nothing else, it wrote directly into `DECISIONS.md`, pruned `open-questions.md`, and stamped the readiness doc "answered in full" when John had not finished. Its entries were good enough to keep but three of its mechanics were wrong against live data (DEC-066 evidence, DEC-069, DEC-074 — each first draft would have alerted every subscriber every morning). **Verify claims against the data and the log; never take "done" from a report at face value.**
- **The NYC Open Data host is reachable when the session runs on John's machine** (Remote Control), not from cloud sandboxes. `curl` it. Aggregate group-bys take 30–140 s; plate queries take 0.3 s. Probe first, then commit responses as fixtures.
- **John works from his phone on Remote Control.** He does not open files. Put lists in chat, compact, phone-readable; use `SendUserFile` for anything he might want to hand to another session. One question at a time; he answers by exception ("confirm all except…"). He pushes back with reasons and expects the same.
- **John's plate has seven open tickets** as of 2026-09-20 (two accruing penalties). It is the primary fixture and will be the first live subscription.

## Your job, in order

1. **Establish state.** `git log --oneline main..HEAD`; `git status`; `ls docs/handoffs/`. Read `docs/handoffs/2026-09-20-cascade.md` (its `Status:` line first) and, if it exists, `2026-09-20-cascade-report.md`.
   - Cascade not started → tell John; offer to run it yourself (you can — it is editorial — but a cheaper session was the plan).
   - Cascade partial → read what landed, assess whether to finish it or reset the branch to `966e8a1`'s successor and re-run. Say which and why in one line; don't ask.
   - Cascade done → continue.
2. **Verify the cascade independently of its report.** Run the brief's stale-term grep yourself. Spot-check, against `DECISIONS.md` §9: PRD §4, §6 (send types, outcome model, job, retries, exit criteria), §7, §9; `CLAUDE.md` invariants 1 and 5 and the Phase 1 rules; `docs/messaging.md` drafts (no nickname, no Alert, "tickets"); `docs/open-data-reference.md` counting rules. Confirm the off-limits files are untouched: `git diff 762761b..HEAD --stat -- DECISIONS.md docs/mvp-readiness.md docs/citypay-reference.md docs/fixtures` must show nothing but the handoffs move. Read every `<!-- TODO(john) -->`.
3. **Put the results in front of John in chat:** the TODO list (each with your recommended resolution), the grep survivors you judge wrong (not the legitimate ones), and any place the cascade quietly decided something. One message, then one question: fix these and merge?
4. **On his word:** apply the fixes (one commit, `Cascade DEC-064–081: review fixes`), add a `DECISIONS.md` entry only if a TODO resolution is a genuinely new decision, flip `2026-09-20-cascade.md` and this file to `Status: done`, update the handoffs README index, and merge to `main` — trunk-based, no PR ceremony (P13): `git checkout main && git merge --ff-only phase1-readiness-review` (or a merge commit if ff is impossible). Push only if John says push.
5. **Then start the build,** per `docs/open-questions.md` §F and the README's "Next": `rails new` (Rails 8, Postgres, RSpec, RuboCop, GitHub Actions), fixtures to `spec/fixtures/open_data/`, `CLAUDE.md` Commands section filled. Before generating anything, ask John one question: whether he wants the Twilio step-zero done first or in parallel — it is his action, not yours, and it gates live sends, not the build. Write a new handoff (`YYYY-MM-DD-build-skeleton.md`) before you stop, so the next session can resume the build the same way you resumed this.

## Invariants that bind everything above

`CLAUDE.md` is authoritative. In particular: never a false all-clear in the record — a broken read is never logged as Clean and never produces a digest; never a send without a live subscription; logs carry subscription id + plate, never phone numbers; proposed ≠ decided — anything John hasn't explicitly confirmed is marked Proposed and does not bind; and when a non-obvious decision lands, a `DECISIONS.md` entry goes in the same commit.
