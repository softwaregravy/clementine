# mise pins the Ruby from .ruby-version; this puts the project's binstubs on PATH so
# rails/rake/rspec run without `bundle exec`, and loads .env for psql and plain rake
# (dotenv-rails covers the Rails processes).
PATH_add bin
dotenv_if_exists
